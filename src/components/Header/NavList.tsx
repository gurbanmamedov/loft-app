
import NavItem from './NavItem'

const NavList = () => {
    return (
        <ul className="hidden md:flex justify-center items-center gap-5 z-[50]">
            <NavItem />
        </ul>
    )
}

export default NavList