
import NavBar from "./NavBar"

const Header = () => {
    return (
        <header className='fixed top-0 w-full backdrop-blur-sm z-20'>
            <NavBar />
        </header>
    )
}

export default Header