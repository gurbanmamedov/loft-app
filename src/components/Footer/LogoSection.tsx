import logoImage from '../../assets/logo.svg';

const LogoSection = () => {
    return (
        <a href="/">
            <img src={logoImage} alt="Logo" />
        </a>
    );
};

export default LogoSection;
